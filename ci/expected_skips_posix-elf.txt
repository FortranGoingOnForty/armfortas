# Suites expected to skip on hosts without the native toolchain
# (posix-elf profile). Regenerated after x10 enabled the vectorize
# suites on x86_64: what remains is Mach-O artifact audits, the
# afs-as/afs-ld standalone toolchain (x13/x14), reference-compiler
# differentials, and cli_driver's ELF-gated -shared test.
abi_differential
artifact_audit_29_10
cli_driver
standalone_toolchain
testing
