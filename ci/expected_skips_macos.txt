# Suites expected to skip on macOS (macos profile): tests that need
# an x86_64 ELF host. Everything else must run — zero other skips.
x86_emit_golden
abi_differential
x86_object_smoke
x86_afs_as_differential
x86_complex_value_runtime
elf_link_e2e
elf_static_link
